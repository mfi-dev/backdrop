<div class="rhythm row {{ body_classes }}">
    <div class="col-md-8 col-lg-6">
        {% if page.title %}
            <h2>{{ page.title|e('html') }}</h2>
        {% endif %}
        {{ content|raw }}
    </div>
</div>
